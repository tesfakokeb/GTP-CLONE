# Backend site root (trailing slash optional). API calls use {base}/api/...
VITE_API_BASE_URL=https://gtp-clone-bk.vercel.app/

# Local dev with Vite proxy: comment out the line above, or use:
# VITE_API_BASE_URL=
